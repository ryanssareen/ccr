{
  "name": "ccr-monorepo",
  "version": "0.0.0",
  "description": "CCR monorepo — terminal CLI, shared core, and (forthcoming) Electron desktop app.",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:desktop": "npm run -w @ccr/desktop dev",
    "build:desktop": "npm run -w @ccr/desktop build",
    "build": "npm run -w @ccr/core build && npm run -w @ryanisavibecoder/ccr build && npm run -w @ccr/desktop build",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
