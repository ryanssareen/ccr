// Single source of truth for the runtime version string. Mirrors the
// `version` field in package.json — bump both together when releasing.
export const VERSION = "1.4.1";
export const PACKAGE_NAME = "@ryanisavibecoder/ccr";
