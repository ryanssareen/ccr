{
  "name": "@ccr/core",
  "version": "1.4.5",
  "description": "Shared agent loop, tool registry, proxy client, and session I/O for CCR. Consumed by the CLI and the Electron desktop app.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "diff": "^5.2.0",
    "openai": "^4.79.0"
  },
  "devDependencies": {
    "@types/diff": "^5.2.3"
  },
  "engines": {
    "node": ">=18"
  }
}
